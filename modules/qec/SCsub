Import('env')

qec_env = env.Clone()
qec_env.add_source_files(env.modules_sources, "*.cc")
# Append CCFLAGS flags for both C and C++ code.
if env["platform"] == "windows":
    qec_env.Append(CXXFLAGS=['-fexceptions', '/std:c++20'])
else:
    qec_env.Append(CXXFLAGS=['-fexceptions', '-std=c++20'])
